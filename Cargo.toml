[package]
name = "fold_gtrace"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "3.1", features = ["cargo", "deprecated", "std"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0", features = ["std"] }

[profile.release]
lto = true
